
public enum Gender {
	MALE, FEMALE, UNKNOWN;

	/*
	 * khởi tạo giá trị là số MALE(0), FEMALE(1), UNKNOWN(2);
	 * 
	 * private int numVal; Gender(int numVal) { this.numVal = numVal; }
	 * 
	 * public int getNumVal() { return numVal; }
	 */
}
